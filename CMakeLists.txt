cmake_minimum_required(VERSION 3.18)

project(AGATE_UNITS
  VERSION 0.1.0
  LANGUAGES C
)

set(AGATE_UNIT_BASE_DIRECTORY "${CMAKE_INSTALL_PREFIX}/share/agate")

install(
  DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/units"
  DESTINATION "${AGATE_UNIT_BASE_DIRECTORY}"
)
